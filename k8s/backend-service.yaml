apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: medical-appointment
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: medical-appointment
    tier: backend
